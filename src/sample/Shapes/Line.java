/*
package sample;

import javafx.scene.canvas.GraphicsContext;
import javafx.scene.paint.Color;
import javafx.scene.shape.Shape;

public class Line extends Shape {

// classe ligne pour les lignes et futur polygones

    private double startX;
    private double startY;

    private double width;

    public Line(double width, boolean filled, Color color, Color fill, double X, double Y, double W){
        super(color, width, filled, fill);
        this.startX = X;
        this.startY = Y;
        this.width = W;
    }

    @Override
    public void Draw(GraphicsContext gc) {
        gc.setStroke(this.getWidth(), this.getColor());
        gc.strokeLine(this.StartX, this.StartY);
    }


    public double getStartX() {
        return startX;
    }

    public void setStartX(double startX) {
        this.startX = startX;
    }

    public double getStartY() {
        return startY;
    }

    public void setStartY(double startY) {
        this.startY = startY;
    }

    public double getWidth() {
        return width;
    }

    public void setWidth(double width) {
        this.width = width;
    }

}*/