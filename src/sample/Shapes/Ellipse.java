/*
package sample.Shapes;

import javafx.scene.canvas.GraphicsContext;
import javafx.scene.paint.Color;
import javafx.scene.shape.Shape;

public class Ellipse extends Shape {
    private double startX;
    private double startY;

    private double width;
    private double radius;

    public Ellipse(double width, double radius, boolean filled, Color color, Color fill, double X, double Y, double W, double R){
        super(color, width, filled, fill);
        this.startX = X;
        this.startY = Y;
        this.width = W;
        this.radius = R;
    }

        @Override

    public void Draw(GraphicsContext gc) {
        gc.setStroke(this.getWidth(), this.getColor());
        gc.strokeLine(this.StartX, this.StartY);

    public double getStartX() {
        return startX;
    }

    public void setStartX(double startX) {
        this.startX = startX;
    }

    public double getStartY() {
        return startY;
    }

    public void setStartY(double startY) {
        this.startY = startY;
    }

    public double getWidth() {
        return width;
    }

    public void setWidth(double width) {
        this.width = width;
    }

    public double getRadius() {
        return radius;
    }

    public void setRadius(double radius) {
        this.radius = radius;
    }
    */