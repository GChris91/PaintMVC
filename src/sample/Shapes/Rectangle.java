/*
package sample.Shapes;

import javafx.scene.canvas.GraphicsContext;
import javafx.scene.paint.Color;
import javafx.scene.shape.Shape;

public class Rectangle extends Shape {

    private double startX;
    private double startY;

    private double width;
    private double height;

    public Rectangle(double width, double height, boolean filled, Color color, Color fill, double X, double Y, double W, double H){
        super(color, width, filled, fill);
        this.startX = X;
        this.startY = Y;
        this.width = W;
        this.height = H;
    }

    @Override
    public void Draw(GraphicsContext gc) {
        gc.setStroke(this.getWidth(), this.getColor());
        gc.strokeLine(this.StartX, this.StartY);
    }


    public double getStartX() {
        return startX;
    }

    public void setStartX(double startX) {
        this.startX = startX;
    }

    public double getStartY() {
        return startY;
    }

    public void setStartY(double startY) {
        this.startY = startY;
    }

    public double getWidth() {
        return width;
    }

    public void setWidth(double width) {
        this.width = width;
    }

    public double getHeight() {
        return height;
    }

    public void setHeight(double height) {
        this.height = height;
    }


}
*/