package sample;

import javafx.scene.canvas.GraphicsContext;
import javafx.scene.paint.Color;
/*
public class Line extends Shape{

    private double StartX ;
    private double StartY ;
    private double EndX;
    private double EndY;

    public Line(Color color, double EndX, double EndY, double StartX, double StartY){
        super(color);
        this.EndX = EndX;
        this.EndY = EndY;
        this.StartX = StartX;
        this.StartY = StartY;
    }

    @Override
    public void Draw(GraphicsContext gc) {
        gc.setStroke(this.getColor()); //add .getValue() for epaisseur
        gc.strokeLine(this.StartX, this.StartY, this.EndX, this.EndY);
    }


    public double getX() {
        return X;
    }

    public void setX(double x) {
        X = x;
    }

    public double getY() {
        return Y;
    }

    public void setY(double y) {
        Y = y;
    }
}
*/c